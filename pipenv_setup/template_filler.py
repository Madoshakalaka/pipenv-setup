from typing import Dict

from pipenv_setup.constants import Config

# todo: parse remote packages from pipfile.lock instead
def fill_template(remote_packages: Dict[str, Config]):
    """
    fill res/setup_template.py, prints to console

    """
    pass
